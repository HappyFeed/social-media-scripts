# Requirements Document: [Feature Name]

## Introduction

[1-2 paragraphs: what this feature is, why it exists, and what it
depends on or connects to in the existing system. Link back to the
brainstorming discussion if useful context lives there.]

## Requirements

### Requirement 1: [Short, specific name]

**User Story:** As a [role], I want [capability], so that [benefit].

#### Acceptance Criteria

1. WHEN [trigger/event] THE SYSTEM SHALL [expected response]
2. IF [unwanted condition or error case] THEN THE SYSTEM SHALL [expected response]
3. WHILE [ongoing state, if relevant] THE SYSTEM SHALL [expected response]

### Requirement 2: [Short, specific name]

**User Story:** As a [role], I want [capability], so that [benefit].

#### Acceptance Criteria

1. WHEN [trigger/event] THE SYSTEM SHALL [expected response]
2. IF [unwanted condition or error case] THEN THE SYSTEM SHALL [expected response]

<!-- Add one "### Requirement N" block per distinct behavior. Keep
each acceptance criterion to one testable sentence — if a criterion
needs "and", it's probably two criteria. -->

## Out of Scope

- [Behavior explicitly excluded from this feature, so it doesn't
  quietly get added back in during design or implementation]
